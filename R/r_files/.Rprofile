# Friendly startup message
message("\nWelcome to R! ", Sys.info()[["user"]], " - ", date(), "\n", "This is your project: ", basename(getwd()), "\n")

options(
    styler.addins_style_transformer = "styler::tidyverse_style(indent_by = 4L)"
)
